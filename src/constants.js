/**
 * @desc
 * API types
 */
const CALL_API = 'CALL_API';
const CHAIN_API = 'CHAIN_API';
const PARALLEL_API = 'PARALLEL_API';

/**
 * @desc Dispatch mode
 * Should dispatch on every request or dispatch only once?
 */
const DISPATCH_ONCE = 'DISPATCH_ONCE';
const DISPATCH_EVERY = 'DISPATCH_EVERY';

export {
  CALL_API,
  CHAIN_API,
  PARALLEL_API,

  DISPATCH_ONCE,
  DISPATCH_EVERY
};